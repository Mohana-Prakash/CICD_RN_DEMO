# Dependencies
node_modules/

# React Native
.expo/
.expo-shared/

# Android — ignore only generated/local files, not the project itself
android/.gradle/
android/app/build/
*.keystore
!debug.keystore

# iOS — ignore only generated/local files, not the project itself
ios/Pods/
ios/build/

# Build output
build/
dist/

# Metro
.metro-health-check*

# Misc
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.*.local

# TypeScript
*.tsbuildinfo

# VSCode
.vscode/
